export const CONFIG_PATH = '.poweron-pipelines/config.yml';

/* Default values */

export const DEFAULT_POWERON_DIRECTORY = 'REPWRITERSPECS/';
export const DEFAULT_SSH_PORT = '22';
export const DEFAULT_TIMEOUT = 30000; // 30 seconds

/* Patterns */

export const TARGET_BRANCH_PATTERN = /^refs\/heads\/.+$/;
